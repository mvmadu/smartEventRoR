<h1>Editing user</h1>

<%= render 'new' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %> |
<%= link_to 'Destroy', "/users/#{@user.id}/destroy", method: :delete, data: { confirm: 'Are you sure?' } %>